<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- tool_type -->
<mapper namespace="com.zgh.onlinevideo.dao.ToolTypeDao">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="toolTypeMap" type="ToolType">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="flag" property="flag" jdbcType="VARCHAR" />
    </resultMap>
    <!-- This code was generated by TableGo tools, mark 1 end. -->
    
    <!-- This code was generated by TableGo tools, mark 2 begin. -->
    <!-- 表查询字段 -->
    <sql id="allColumns">
        tt.id, tt.name, tt.flag
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->
    
    <!-- 查询所有数据 -->
    <select id="findToolTypeAll" resultMap="toolTypeMap">
        SELECT
        <include refid="allColumns" />
        FROM tool_type tt
    </select>
    
    <!-- 根据条件参数查询数据列表 -->
    <select id="findToolTypeByCondition" resultMap="toolTypeMap" parameterType="map">
        SELECT
        <include refid="allColumns" />
        FROM tool_type tt WHERE 1 = 1
        <if test="name != null and name != ''">
            AND tt.name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="flag != null">
            AND tt.flag = #{flag}
        </if>
    </select>
    
    <!-- 根据主键查询数据 -->
    <select id="findToolTypeByIds" resultMap="toolTypeMap" parameterType="list">
        SELECT
        <include refid="allColumns" />
        FROM tool_type tt WHERE tt.id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
    
    <!-- 插入数据 -->
    <insert id="insertToolType" parameterType="toolType">
        INSERT INTO tool_type (
            id, name, flag
        ) VALUES (
            #{id},
            #{name},
            #{flag}
        )
    </insert>
    
    <!-- 修改数据 -->
    <update id="updateToolType" parameterType="map">
        UPDATE tool_type
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="flag != null">
                flag = #{flag}
            </if>
        </set>
        WHERE id = #{id}
    </update>
    
    <!-- 批量修改数据 -->
    <update id="updateToolTypes" parameterType="list">
        <foreach collection="list" index="index" item="item" separator=";">
            UPDATE tool_type
            <set>
                <if test="item.name != null">
                    name = #{item.name},
                </if>
                <if test="item.flag != null">
                    flag = #{item.flag}
                </if>
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>
    
    <!-- 根据主键删除数据 -->
    <delete id="deleteToolTypeById" parameterType="int">
        DELETE FROM tool_type WHERE id = #{id}
    </delete>
    
    <!-- 根据主键批量删除数据 -->
    <delete id="deleteToolTypeByIds" parameterType="list">
        DELETE FROM tool_type WHERE id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>