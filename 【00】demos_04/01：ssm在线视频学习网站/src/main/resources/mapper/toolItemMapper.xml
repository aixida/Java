<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- tool_item -->
<mapper namespace="com.zgh.onlinevideo.dao.ToolItemDao">
    <!-- This code was generated by TableGo tools, mark 1 begin. -->
    <!-- 字段映射 -->
    <resultMap id="toolItemMap" type="ToolItem">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="tool_url" property="toolUrl" jdbcType="VARCHAR" />
        <result column="cover_url" property="coverUrl" jdbcType="VARCHAR" />
        <result column="tool_type_id" property="toolTypeId" jdbcType="INTEGER" />
    </resultMap>
    <!-- This code was generated by TableGo tools, mark 1 end. -->
    
    <!-- This code was generated by TableGo tools, mark 2 begin. -->
    <!-- 表查询字段 -->
    <sql id="allColumns">
        ti.id, ti.name, ti.tool_url, ti.cover_url, ti.tool_type_id
    </sql>
    <!-- This code was generated by TableGo tools, mark 2 end. -->
    
    <!-- 查询所有数据 -->
    <select id="findToolItemAll" resultMap="toolItemMap">
        SELECT
        <include refid="allColumns" />
        FROM tool_item ti
    </select>
    
    <!-- 根据条件参数查询数据列表 -->
    <select id="findToolItemByCondition" resultMap="toolItemMap" parameterType="map">
        SELECT
        <include refid="allColumns" />
        FROM tool_item ti WHERE 1 = 1
        <if test="name != null and name != ''">
            AND ti.name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="toolUrl != null and toolUrl != ''">
            AND ti.tool_url LIKE CONCAT('%', #{toolUrl}, '%')
        </if>
        <if test="coverUrl != null and coverUrl != ''">
            AND ti.cover_url LIKE CONCAT('%', #{coverUrl}, '%')
        </if>
        <if test="toolTypeId != null">
            AND ti.tool_type_id = #{toolTypeId}
        </if>
    </select>
    
    <!-- 根据主键查询数据 -->
    <select id="findToolItemByIds" resultMap="toolItemMap" parameterType="list">
        SELECT
        <include refid="allColumns" />
        FROM tool_item ti WHERE ti.id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
    
    <!-- 插入数据 -->
    <insert id="insertToolItem" parameterType="toolItem">
        INSERT INTO tool_item (
            id, name, tool_url, cover_url, tool_type_id
        ) VALUES (
            #{id},
            #{name},
            #{toolUrl},
            #{coverUrl},
            #{toolTypeId}
        )
    </insert>
    
    <!-- 修改数据 -->
    <update id="updateToolItem" parameterType="map">
        UPDATE tool_item
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="toolUrl != null">
                tool_url = #{toolUrl},
            </if>
            <if test="coverUrl != null">
                cover_url = #{coverUrl},
            </if>
            <if test="toolTypeId != null">
                tool_type_id = #{toolTypeId}
            </if>
        </set>
        WHERE id = #{id}
    </update>
    
    <!-- 批量修改数据 -->
    <update id="updateToolItems" parameterType="list">
        <foreach collection="list" index="index" item="item" separator=";">
            UPDATE tool_item
            <set>
                <if test="item.name != null">
                    name = #{item.name},
                </if>
                <if test="item.toolUrl != null">
                    tool_url = #{item.toolUrl},
                </if>
                <if test="item.coverUrl != null">
                    cover_url = #{item.coverUrl},
                </if>
                <if test="item.toolTypeId != null">
                    tool_type_id = #{item.toolTypeId}
                </if>
            </set>
            WHERE id = #{item.id}
        </foreach>
    </update>
    
    <!-- 根据主键删除数据 -->
    <delete id="deleteToolItemById" parameterType="int">
        DELETE FROM tool_item WHERE id = #{id}
    </delete>
    
    <!-- 根据主键批量删除数据 -->
    <delete id="deleteToolItemByIds" parameterType="list">
        DELETE FROM tool_item WHERE id IN
        <foreach collection="list" index="index" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>